import { pallette, typography } from '@packages/web/themes/default/settings';

export const Menu = `
  background-color: ${pallette.smoke};
  font-family: ${typography.fontFamilyDefault};
`;

export const Brand = `
  border-right: 1px solid ${pallette.iron};
`;
