import { spacing } from '@packages/web/themes/default/settings';

export const List = `
  margin: 0;
  padding: 0;
`;

export const Item = `
  list-style: none;
  margin: 0;
  padding: 0;
`;

export const ItemLink = `
  display: block;
  padding: ${spacing.default} ${spacing.spacious};
`;

export const ItemText = `
  display: block;
  padding: ${spacing.default} ${spacing.spacious};
`;
